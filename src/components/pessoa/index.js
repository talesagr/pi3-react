import React from "react";
import './styles.css';

const Pessoa = () => {
    return (
        <div className="textAreaPessoa">
            <textarea className="textAreaPessoa" disabled></textarea>
        </div>
    );
}

export default Pessoa;